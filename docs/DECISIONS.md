# DECISIONS.md — 実装中に決めたこと

## 2026-08-24（3日目標・自律実装）

- **距離減衰の基準距離**: SPEC 6.1「発射時距離」は発射時点で相手が未確定のため、実装は **発射位置→命中位置の弾の飛行距離** で判定。実質同じ挙動。差が出るのは相手が弾に向かって走った場合のみ（少し有利判定）。
- **中心ヒット判定**: 弾は1tickに約23unit進むため、「命中時点の距離」だと中心を通る弾でも中心判定にならない。**弾道（直線）と中心の垂直距離**で判定するよう変更（テスト「中心ヒットは1.5倍」で検出）。当たり判定自体は1tickの移動線分で行い、すり抜けを防止。
- **ガード中の被弾**: 三すくみ「射撃＞防御」を「ダメージ0・ゲージ-25」と解釈。ゲージ0で自動的にガード解除＋0.6秒ブレイク。ガードブレイク中は移動・射撃・防御不可。
- **ライフスティール**: Day1は全キャラ共通の「命中で与ダメ50%シールド回復」のみ。スピード型固有の「背面180度限定＋秒間キャップ12」はセイバー実装時（2週間目標）に扱う。
- **被ダメ倍率**: Day1は1.0（標準）。キャラ実装時に1.3/0.85/1.0へ。
- **撃破時スロー0.3秒**: ホストのシミュレーション速度を0.3倍にして実現（見た目だけでなく全員同期のスロー）。
- **中継サーバーのビルド**: tsc の NodeNext だと shared の相対importに拡張子が必要になるため、esbuild でバンドル（`--packages=external`）。
- **フィールド寸法**: 論理1280×720。「画面幅」の%はこの1280を基準。
- **練習モード**: 動作確認用に「的（動かないプレイヤー）」と1人で戦えるモードをタイトルに追加。SPECにはないが、非エンジニアの確認導線として有用なので残す（2週間目標のbot Lv1に吸収予定）。
- **ルームコードの文字**: 0/O/1/I を除いた英数字32種（口頭で伝えやすくするため）。
- **非ホスト切断**: 中継からの members 更新をホストが検知し、該当プレイヤーの残機を0にして退場扱い（試合継続）。

## 2週間目標・キャラ実装（今回）
- 支援型の武器は「スナイパー→回復弾→素手」の3枠サイクル（SPEC 6.3の武器欄が3つのため。切替0.3秒は共通）
- かばうの1vs1・乱闘フォールバックは「前方ダッシュ110＋1秒シェル（被ダメ60%カット）」として実装（SPEC 6.2の詳細未規定分。調整枠）
- 鈴の無敵は復帰無敵と同じフィールドを流用。攻撃すると解除される（調整枠として記録）
- 範囲回復は自分対象外（SPEC 6.3「HP回復手段は自身へは素手のみ」に合わせた）。1vs1・乱闘では実質死にスキル→2vs2で本領
- HMGの拡散リセットは停止2秒後に一括リセット（スピン維持1秒＋リセット1秒。漸進リセットは未実装・調整枠）
- 弾の壁反射後は「発射位置」を反射点に置き直す（ピストル距離減衰の基準。支援弾は減衰なしだが将来のため統一）
- 反射弾の押し出し距離は壁厚/2＋弾半径＋2（貼り付き二重判定の防止）
- スピード型の切り返し硬直（移動方向転換のもたつき）は未実装。PLAN の残項目に記載
- キャラ選択はロビーで各自が pick をブロードキャスト。ホストが start に確定クラスを載せる。ミラー許可（SPEC 4章）

## 2vs2・CPU bot 実装（今回）
- 【ユーザー裁定・裁定7】CPU botはLv3まで前倒し実装（SPEC 12章の「v1はLv2まで」を上書き。SPEC本文の更新は未実施＝ユーザー確認待ち）
- チーム残機5共有: 撃破でプール減→残があればリスポーン、プール0なら永久退場。両名退場でチーム敗北
- 時間切れ判定（2vs2）: チーム残機差→生存メンバーの残HP合計差（シールド除外）→引き分け
- 弔い合戦: CD半減は「CD消化速度2倍」で実装。ゲージ制は逃げ/統合ゲージの回復速度2倍。ガードゲージは対象外（防御性能の直接強化になるため。調整枠）
- かばう本実装: カーソル方向に最も近い生存味方へ吸着ダッシュ（上限300・手前で停止）＋3秒シェル。シェルは自分に付く（身を挺して射線に入る運用）
- botの難易度はLv1/2/3の3プリセットに統合（エイム誤差0.2/0.09/0.045rad・反応0.5/0.25/0.12秒・スキル頻度0/0.6/0.9）。SPEC「3パラメータのみ」の範囲内
- bot Lv3のチーム行動: 集中砲火（実質耐久最低の敵狙い）・支援は味方HP60%未満で回復弾優先・重量はHP35未満の近接味方へかばう・弔い合戦中は後退しない
- botのスナイパーは溜め1.1秒（Lv1は0.6秒）でリリース
- 2vs2のチーム分けは並び順固定（上2人vs下2人）。入替UIは未実装（PLAN残項目）
- ロビーのbot操作はホスト専用。「botのLv/キャラ」ボタンで最後のbotをLv1→2→3→次キャラ…と巡回

## 3v3＋v1残項目の実装（今回）
- 【ユーザー裁定・裁定8】3v3を実装。チーム残機は7（暫定・調整枠）、フィールドは1280×720据え置き（広さ変更は「画面幅%」基準の全数値に波及するため見送り。手狭なら調整枠）
- チーム戦のスポーンを左列vs右列に変更（従来は2vs2で味方が対角に湧くバグがあった）
- 【裁定に準ずる実装判断】フレンドリーファイアなし（回復弾以外は味方に当たらない）。SPECに明記がないが、チーム戦・層2反射の成立に必須のため。SPEC追記候補
- 切り返し硬直: 角度スケールを45°=0.03秒〜180°=0.1秒の線形補間、発動しきい値40°、移動のみ停止（照準・攻撃は可＝入力常時受付）、停止からの動き出しは硬直なし、高速移動後0.2秒免除
- 層2は3ペアとも効果まで実装（裁定6の「枠組みのみ」を超過達成）。成立判定はスキル使用記録の突き合わせ（同チーム・別人・0.5秒以内・320px以内）、1発動につき1成立
- ミストシグナルの実用形は「スモークを先に置き、0.3〜0.45秒遅らせてスタン弾を撃ち込む」。構え0.3秒の仕様上、同時発動だと弾がスモークを抜けてしまうため（数値はSPEC通り。運用をREADMEに書く候補）
- リザルトの「最大連携ダメージ」表示は未実装（連携回数のみ）。v1.1へ
- SEはアセットなしのWebAudio合成（発射・ヒット・中心・ガード・ブレイク・撃破・回復・味方ダウン・LINK和音）。連続ヒットのピッチ上昇はコンボ1秒窓
- ヒットストップはホストのsim減速0.04秒（中心ヒット・近接のみ）。全員に同期がかかる方式
- 遅延補正は「自機移動のクライアント予測＋権威位置への12%/フレーム吸着」。他プレイヤーは従来どおり補間。攻撃・スキルの予測はなし（v2候補）
- キー設定はlocalStorage保存・重複割り当ては自動入れ替え。マウス系は固定
- 称号は簡易ルール（勝利×与ダメ1位=アリーナの主役 / 撃破1位=ネオンの死神 / LINKあり=シンクロニスト / 与ダメ1位=火力の化身 / 被撃破1位=不死身の挑戦者 / それ以外=堅実な仕事人）。文言は調整枠
- 公開はRender（relay・render.yaml同梱）＋Cloudflare Pages（client）。手順はREADME

## v1残項目の完了（今回）
- 最大連携ダメージ: LINK成立後3秒間の参加2名の与ダメ合計を窓ごとに集計し最大値を採用（窓の長さは実装判断＝調整枠）。リザルトに表示
- ビッグヒット判定: 中心ヒットかつダメ40以上（最大溜め中心クラス）、または同一対象への近接0.6秒内4ヒット（セイバー全段）。専用SE＋強シェイク＋火花
- 撃破ズーム: スロー0.3秒に加えカメラ1.1倍ズーム→復帰
- 残り1機: FFAは残機1の個人、チーム戦はプール1のチーム全員がゆっくり点滅。自分側がピンチのときBGMがテンポアップ＋音程上昇
- BGMはWebAudio合成の2音パルスループ（アセットなし・音量控えめ）
- 処理落ち検知（安全弁・SPEC 10章）: ホストのフレーム平均40ms超で演出簡略化（火花・LINKエフェクト削減）＋バナー告知。bot削減までは踏み込まず（v1.1判断）
- チーム分け入替: ロビーの並び順ローテーション方式（人間の並びは中継準拠のため、リスト全体を回す）。ホスト専用
- 【未実装・ユーザー裁定待ち】支援型特性「静穏オーラ」: SPEC 6.3に名前のみで定義が存在しない。効果の定義をもらってから実装する

## 静穏オーラ（裁定9・ユーザー決定）
- 効果: 半径96px（2キャラ分）・味方HP毎秒2・最終被弾から4秒経過のみ・シールド対象外・支援型ダウン中は消滅
- 自己回復: あり（Claudeも賛成）。SPEC 5.4の「自身へは素手のみ」に例外1行を追記して整合
- 表示: パッシブの薄い円のみ（敵にも見える・世界側描画）。tick数字なし（Claudeも賛成）
- 重複: 非スタック（Claude判断・団子誘発防止の意図から安全側。変更容易）
- SPEC本文（6.3・5.4）に反映済み。版数はv3.2のまま（次回校正でv3.3に繰り上げ推奨）

## ドーパミン層の強化（今回・批判レビュー対応）
- 被弾フラッシュ: 被弾側キャラを90ms白く点灯（最重要の追加。全ヒット対象）
- SE刷新: ノイズバースト＋低音サブ（ピッチ急降下）の合成で打撃感を付与。ヒット/中心/ガード/ブレイク/撃破/ビッグヒット全て差し替え。連続ヒットのピッチ上昇はノイズ帯域も一緒に上げる方式
- ヒットストップを「描画フリーズ」方式に変更: sim・通信は止めず描画のみ30〜90ms停止。ホスト・非ホストに等しく効く（旧ホストsim減速方式は撃破スローのみに限定して継続）
- 毎ヒットの着弾火花（4〜6粒・中心は黄色）＋発射時の銃口フラッシュを追加。lowSpec時は自動オフ
- ダメージ数字をダメージ量に比例したサイズに（16+0.75×ダメ、上限48px）
- BGMに簡易ハイハット（ノイズ）を追加。音量は全体に控えめ維持

## 音量設定（今回）
- 設定画面（キー設定と同居）にBGM/SE個別の音量調節（◀▶で10%刻み・0〜100%）。localStorage保存
- SE音量の変更時はヒット音を試し鳴らし。0%は発音自体をスキップ（負荷ゼロ）

## 武器を左右クリックに統一（裁定10・ユーザー決定）
- 武器切替という操作を廃止。**左クリック＝主武器 / 右クリック＝副武器**の2枠に統一
  - スピード型: 左セイバー / 右ピストル
  - 重量型: 左HMG / 右ナイフ
  - 支援型: 左「狙撃/ヒール」融合枠 / 右ジャブ
- 支援型は武器3種で2ボタンに載らないため、狙撃とヒールを左クリックに融合。離したときの溜め時間で3分岐:
  - 単クリック（< 0.15秒）= ヒール
  - 0.15〜0.3秒 = 不発（溜めフェイントを維持するための区間・ユーザー案）
  - 0.3秒以上 = 狙撃（威力は溜め時間に比例。従来どおり連続スケール、段階制にはしない）
- 廃止に伴い `weaponSwitchSeconds` と切替クールダウン中の攻撃ロックを削除（「切替直後に攻撃が出ない」不具合の解消を兼ねる）
- キー設定から「武器切替」の行を削除
- 【Claude提案・不採用】ガードで溜めキャンセル → ユーザー案（単クリック分岐）の方が追加ルール不要で優れるため棄却

## 入力ラッチ方式（裁定11）
- 症状: スキル・武器切替が不発になることがある
- 原因: `JustDown` を毎フレーム読んで `inputs` を上書きしていたため、tickが走らないフレームで拾ったエッジが次フレームに踏み潰される。描画は144Hz等でも sim は60Hz固定なので、高リフレッシュレート環境ほど頻発。ゲスト側も送信60Hzで同じ取りこぼし
- 対処: 押した瞬間をラッチ（フラグ保持）し、**tick／送信が必ず1回消費してから消す**方式へ変更
- 【Claude提案・ユーザー承認】tickRateの144Hzへの引き上げは棄却。原因がレートではなく取りこぼしであること、ホスト権威型でホストPCの負荷が2.4倍になること、既存の裁定・テスト70本が60Hz前提であることが理由。60Hzでの遅延は最大16.7msで体感不能

## 世界スケール 2/3（裁定12・ユーザー決定）
- キャラ・武器リーチ・スキル範囲・静穏オーラ・弾サイズ・弾速・移動速度を**一律 2/3**に縮小。時間とダメージは不変
- フィールド寸法(1280×720)は据え置きのため、キャラ基準ではアリーナが1.5倍広くなる（横断: 速1.8秒/重3.0秒/支2.25秒）
- 間合いの関係（何キャラ分でリーチが届くか）は完全に据え置き＝戦い方は変わらない。「移動している時間の割合が増える」のが唯一の変化
- 実装: `balance.ts` の `WORLD_SCALE` 定数1箇所。`px()` / `ratio()` / `cross()` を通して適用しており、`1` に戻せば旧スケールへ即復帰できる
- 実プレイで散漫に感じた場合はフィールド側を縮める方向で調整する

## 近接を棒の掃き判定に（裁定13・ユーザー決定）
- 旧: 決められた時刻に扇全体が一瞬光る点判定
- 新: **キャラ中心から伸びた棒が扇を掃き、棒が横切った相手に当たる**。判定・見た目の両方を変更
- パス設計（中心を横切る時刻が旧仕様と一致するよう調整＝手触りを維持）:
  - セイバー: **2往復＝4パス**（各0.1秒）。中心通過 0.05/0.15/0.25/0.35秒＝旧hitTimesと完全一致。多段4ヒットを視覚化
  - ナイフ: 1パス（0.15〜0.45秒）。中心通過0.3秒＝旧hitTimeと一致
  - ジャブ: 1パス（0.03〜0.13秒）。中心通過0.08秒＝旧hitTimeと一致
- ダメージ量は不変（単発武器は扇内の相手をいずれ棒が通過するため）。変わるのは扇の端にいる相手に当たるタイミングのみ
- ジャブの扇を **0.7 → 1.0ラジアン**（片側40度→57度）に拡大。範囲が見やすくなる代わりに支援型の近距離自衛は強化側に動くので、実プレイで要観察
- マーク回収の「初撃」判定は、スイング全体の初撃から**相手ごとの初撃**へ変更（掃き方式に整合）

## ESCメニューと訓練場のリセット（裁定14・ユーザー決定）
- 常設の退出ボタンを廃止し、**ESCキーでメニュー**を開く方式に。退出の確認ステップは無し（ユーザー決定）
- メニュー表示中もゲームは止めない（オンラインで止められないため挙動を揃える）。開いている間は操作入力を無効化してクリックの誤爆を防ぐ
- 訓練場: リセット / ゲージ全快 / キー設定 / 閉じる / 訓練場から退出
- 対戦: 閉じる / マッチから退出
- **訓練場に経過時間の概念を持たせない**（HUDの時計を「訓練場」表示に置換）
- リセット = 訓練場に入り直したのと同じ状態。ゲージは**ゼロ**で戻す
- 「ゲージ全快」はリセットとは別項目として独立（ユーザー決定）。リセットと補給は目的が違う操作なので混ぜない
- 【Claude提案・不採用】リセットでゲージ全快 → 実戦のゲージ管理が練習できなくなるため棄却

## キャラクター解説画面（裁定15・ユーザー決定）
- 待機画面に3キャラのアイコンを配置。押すと各キャラの解説画面へ遷移
- 解説画面の上部にもアイコン3つが並び、**開いているキャラのアイコンだけ薄暗い**。押すと切り替わる
- **数値は `balance.ts` から自動生成、文章（役割・立ち回り）だけ手書き**（ユーザー決定）。数値調整をしても説明文が古くならない
- 掲載内容: 基礎値 / 武器2種（左右クリック別）/ スキル3種 / パッシブ / 合体技

## 開始カウントダウン（裁定16）
- 試合開始時に画面中央で 3→2→1→START を表示。数字が変わるたびに効果音＋拡大縮小演出
- **sim側の状態（`SimState.countdown`）として実装**。カウント中は時間経過も含めて一切処理を進めない
  - クライアント側だけの演出にするとホストとゲストでズレるため。スナップショットに載るので全員が同じタイミングで開始する
- 訓練場のリセットでもカウントダウンからやり直しになる
- テストは `createMatch` をラップして `countdown = 0` にし、本編だけを検証する

## 対戦HUDへのキー設定反映（裁定17）
- スキル欄が `E/R/F` 固定だった（リバインドしても表示が変わらない不具合）→ 保存済みのキー設定を参照するよう修正。防御キーも表示に追加
- マウスサイドボタンは `M4` / `M5`、スペースは `SPC` と短縮表示
- 武器欄の「Q/右クリックで切替」（裁定10で廃止済みの操作）を削除し、**左右どちらのクリックで何が出るか**を常時表示に変更
- 支援型の左は「狙撃(溜め)/ヒール(単押し)」と明示

## 試合終了時の分岐（裁定18）
- WIN / LOSE / DRAW の大表示・勝敗理由・称号・戦績は既存実装を維持
- ボタンの文言を用途に合わせて変更: 練習は「続ける」、オンラインのホストは「再戦」（ゲストは「ホストの再戦待ち…」で無効）／もう一方を「ホームに戻る」に統一

## ESCメニューの作り直し（裁定19）
- 症状: メニューの「退出」が効かない
- 原因: メニューを生成して `setVisible(false)` で隠す実装だったため。Phaserでは非表示コンテナの子が入力を拾い続けることがあり、開閉の状態と入力の状態が食い違う
- 対処: **開くたびに生成し、閉じるたびに完全に破棄する**方式へ変更。背面に透明なブロッカーを敷き、メニュー外のクリックがゲームへ抜けないようにした
- ESCをもう一度押すと対戦画面に戻る（メニュー上部にも明記）
- 文言統一: 「閉じる」→「対戦に戻る」、「訓練場から退出／マッチから退出」→「ホームに戻る」

## 重量型の火力調整（裁定20・ユーザー決定）
- HMGのダメージ 4 → **2**（半減）。理由: キャラの機動が速く弾を当てるのが難しい現状では、連射が効くHMGの当てやすさに対して火力が過大
- HMGのマガジン 40 → **30**
- 副作用の相殺: 重量型のシールドは時間回復せず**与ダメージの回収のみ**が回復源のため、ダメージ半減はそのまま耐久半減になる。これを打ち消すため**重量型のシールド回収率を50%→100%**に引き上げ（`shield.heavyLifestealRatio`）
- 統合ゲージは命中「回数」で稼ぐ仕様なのでダメージ半減の影響を受けない（マガジン30化の影響のみ）
- 【Claude提案・不採用】ダメージ4→3の緩やかな調整 → ユーザー判断で半減を採用

## ビルドウォールとグラウンドスラムの操作変更（裁定21・ユーザー決定）
### ビルドウォール（旧「壁」から改名）
- 押した瞬間にゲージ消費して「構え」に入り、**離した瞬間のカーソル位置に設置**
- 設置可能距離は**最大5キャラ分**（`placeMaxPlayers: 5`）。超えた場合はその距離でクランプ。旧仕様は「向いている方向へ固定約1.5キャラ分」だったので、自由配置化と合わせて大幅な強化。強すぎる場合は3キャラ分へ落とす
- 構え中は**移動もHMGも可能**。**右クリックでキャンセル、ゲージは返らない**
  - 重量型の右クリックはナイフだが、構え中はキャンセル入力が優先されナイフは出ない（ユーザー承認済み）
- 構え中は設置予定位置にプレビューを表示（敵にも見える）
### グラウンドスラム
- 発動前に**0.35秒の溜め**を追加。溜め中は範囲円を表示し、内側の円が縮んで着弾を予告する（敵にも見える）
- ゲージは溜め開始時に消費。中断されても返らない
- 【Claude提案・不採用】中断時のゲージ返却 → 「0.35秒にスタンを合わせるのは現実的でない」というユーザー判断で棄却

## 残弾表示（裁定22）
- 画面左下に「残弾 24 / 30」を常時表示。残り25%以下でオレンジ、リロード中は「リロード中 1.2s」赤字に切り替え
- 支援型は弾数管理のある武器を持たないため非表示

## 近接の当たり判定バグ修正（裁定23）
- 症状: 刀・ナイフ・ジャブの「先端に当たり判定がない」ように見える
- 原因: 掃き判定が相手の**中心点の角度**しか見ておらず、相手が半径を持つ円であることを角度側で考慮していなかった。距離は `reach + P.radius` で半径を足していたので、ズレていたのは角度だけ。体が扇に重なっていても中心が扇の外なら当たらず、要求精度が距離によらず一定なので遠い先端ほどシビアに感じた
- 対処: 距離dにおける相手の見込み角 `asin(P.radius / d)` を許容に含める。密着では広く、先端では狭くなり見た目どおりになる
- 副作用対策: 許容を入れると同一パス内で連続tickヒットするため、**1パス1ヒット**の重複排除（`swingPass` / `swingHitIds`）を追加

## クラス速度と各種強化（裁定24・ユーザー決定）
- 横断: スピード **2.1秒** / タンク **2.5秒** / 支援 **2.5秒**（タンクと支援を統一し、スピードだけ少し速い）
- 重量型 → **タンク**に改名
- スモーク → **クラウド**に改名し、**半径2倍**（中で動けるようにしてスキルリンクを活かす）
- 高速移動 → **ソニック**に改名し、距離を**キャラ0.8体分延長**
- ソニックに残像エフェクト: 経路線＋等間隔のシルエット5枚（手前ほど薄い）＋始点の衝撃波リング。移動距離そのものが読める形にした
- 注意: ソニックの距離延長でブリーチの成立距離（画面幅16.7%）を超えやすくなった。繋がりにくければ `link.maxDistanceRatio` を広げる

## 刀（裁定25・ユーザー決定）
- セイバー → **刀**に改名。2往復は見栄えが悪いとの判断で**重厚な一振り（1パス）**に変更
- 押しっぱなしでの連続振りを禁止し、**押すたびに1回**だけ出る（`prevDown` によるエッジ判定）
- 数値調整: 3ダメ×4ヒット=12 → **一振り14**（当て損ないのリスクが上がるぶん微増）。背面シールド回収は 2×4 → **1ヒット8**に集約。振り0.3秒＋硬直0.25秒
- 弾消しの「弾消し!」表示を廃止し、**軽快な金属音のみ**に変更

## 支援型スキルの投擲化（裁定26・ユーザー決定）
### バレットプルーフ（旧・鈴）
- **単押し=自分に使用**／**長押し=味方を選んで投擲**。乱闘でも構えに入れる（練習のため・ユーザー判断）
- 対象選択は**カーソルに一番近い味方を自動選択**（当たり判定を広げるより確実で、密集時も狙いどおり選べる）
- 弾は高速の追尾フラスコ（水色・回転しながら飛ぶ）。着弾で無敵0.75秒＋拘束解除
- 右クリックでキャンセル。対象がいないまま離した場合は不発でCDも消費しない
### ポーション（旧・範囲回復）
- 押す→離すで**カーソル位置へ低速投擲**（緑のフラスコ・回転）。最大キャラ8体分
- 着弾で半径内の味方を20回復。**自分は3割の6回復**（ユーザー決定。SPEC 6.3「自己回復は素手のみ」の例外。裁定9の静穏オーラで既に例外を認めているため方針は一貫）
- スキルリンクの記録は**投げた瞬間**（着弾時ではない）。成立タイミングの感覚を変えないため

## スタン弾ヒット後の狙撃ブースト（裁定27・ユーザー決定）
- 目的: 支援型がスピード型に粘着される状況への対策
- スタン弾を通常ヒットさせると6秒間、**左クリックの単押しで即最大溜めの狙撃**が撃てる
- 威力は通常の最大溜めの**60%**（＝約19）。【Claude指摘】100%だと確定41.6ダメージ（対スピード型）になり、回避不能の一撃必殺になるため
- 【Claude提案・不採用】ノックバックによる引き剥がし → ユーザー判断で狙撃ブーストを採用

## スキルリンクの再編（裁定28・ユーザー決定）
- 「合体技」→ **スキルリンク**に改名
- **エコーウォールを削除**
- ミストシグナル → **ライトニング**に改名（クラウド × スタン弾）
- 追加: **ライトニングスラム**（グラウンドスラム × スタン弾）… スラムののけぞりが切れる前にスタン弾がスラム範囲へ着弾すると、範囲内の敵全員が0.5秒スタン
- 追加: **ヒールスラム**（グラウンドスラム × ポーション）… 同条件で、スラム範囲の味方全員（支援型自身を含む）に追加20回復
- 成立判定は従来の「0.5秒以内の相互発動＋距離」ではなく、**スラム痕跡（`slamZones`）への着弾**で判定する専用ロジック
- 演出: 着弾点を起点にスラム範囲へ一瞬で広がる。ライトニングスラムはジグザグの感電（水色）、ヒールスラムは緑の広がり
- 【Claude指摘・未解決】現状スキルリンク3種のうち2種がスラム起点、ブリーチもタンク起点のため、**タンク不在のチームはスキルリンクが1つも成立しない**。支援型×スピード型のリンク追加は今後の検討事項

## 弔い合戦バナーの位置変更と、固まる不具合の修正（裁定29）
### 表示位置
- 「味方ダウン！CD半減中」が画面中央に大きく出て邪魔だったため、**左上の小さな通知**に変更（色・フォントは維持）
- 同様にシステム系メッセージ（退出・リセット・ゲージ全快・処理落ち検知）も左上の通知に統一
- 中央の大バナーは「FINISH」と「LINK!」だけに絞った

### 固まった原因（裁定28の実装バグ・Claudeの実装ミス）
- `state.slamZones`（スラム×スタン／ポーションのリンク受付枠）を**push するだけで一度も破棄していなかった**
- 結果、スラムを撃つたびに配列が伸び続け、
  1. スタン弾・ポーションの着弾判定が毎tick全件走査になり計算量が増え続ける
  2. `slamZones` は SimState の一部なので**20Hzのスナップショットに丸ごと載り、送信量が膨らみ続ける**
- 試合が長引くチーム戦（＝味方ダウンが起きる状況）ほど顕在化するため、弔い合戦の表示と同時に固まったように見えた
- 対処: 毎tickで**期限切れを破棄**し、あわせて**複製**するよう修正（複製漏れは前フレームの状態を書き換える別のバグでもあった）
- 予防: 角度正規化の `while` ループを `normalizeAngle()` に置換。非有限値が入ると `-Infinity + 2π = -Infinity` で永久に抜けられない構造だったため、ガードを入れた

## スキルリンクの演出（裁定30・ユーザー決定）
- 中央の「LINK! ○○」テキストを廃止。**起きた場所のエフェクトだけ**で見せる
- 内容: 中心の白フラッシュ＋**2色のリングが時間差で広がる**＋2色交互の放射スパーク18本＋軽い画面シェイク
- 色はリンクごとに変える（ブリーチ=水色×橙／ライトニング=水色×桃／ライトニングスラム=水色×橙／ヒールスラム=緑×橙）
- 中央の大バナーに残るのは「FINISH」のみになった

## ビルドウォールの見た目（裁定31・ユーザー決定）
- 厚み（`thickness`）を **1.3倍**（px(12) → px(12×1.3)）。当たり判定は厚みの半分を使っているため、**弾を止める範囲もわずかに広がる**
- 描画を「線1本」から厚みのある構造物へ変更: 外周グロー／本体／上辺ハイライト・下辺シャドウによる立体感／等間隔のリベット4点／端のキャップ
- 耐久が減るほど本体・リベット・キャップが暗くなり、残り耐久が見た目で読めるようにした
- 注意: ここで変えたのは**厚み**。横の長さは `lengthPlayers`（キャラ2.5体分）で別管理なので、長さを伸ばしたい場合はそちらを変更する

## キャラ名の表記とアイコン（裁定32・ユーザー決定）2026-08-25
- 画面上のキャラ名を **スピード／タンク／サポート** に統一（旧「スピード型」「支援型」）。タイトル・ロビー・キャラ解説の3画面が対象
- タイトルとキャラ解説のアイコンは、頭2文字（「スピ」「支援」）の省略をやめて**フルネーム**を表示。円を半径24→30（解説画面は26→30）に広げ、文字12pxで収めた
- `shared` 側のコメントや `SPEC.md` の「支援型」表記はコードの識別子（`support`）と紐づく説明なのでそのまま。変えたのは表示文字だけ

## ソニックの始点リングが飛んでいく不具合（修正・Claudeの実装ミス）2026-08-25
- 症状: ソニック発動時、始点の衝撃波リングが画面の隅へ飛んでいく
- 原因: Phaserの `Graphics` を拡大すると自身の原点(0,0)を基準に拡大される。リングをワールド座標に直接描いたまま `scale` を2.2倍にしていたため、描いた座標ごと2.2倍の位置へ移動していた
- 対処: リングは原点(0,0)に描き、Graphics自体の位置を始点に置く。これでその場で広がる
- 参考: スラムのリンクリング（裁定28）は位置補正で回避していた。今後スケール演出を足すときは同じ罠に注意

## 撃破時にゲームが止まる不具合（修正・Claudeの実装ミス）2026-08-25
- 症状: 撃破が起きると画面が固まり、ESCメニューの「ホームに戻る」なども効かなくなる（「ホームに戻れない」として報告）
- 原因: 撃破ズーム（v1完了時に実装）で `cameras.main.zoomTo(..., "Cubic.Out")` と書いていた。Phaserの**カメラ演出はイーズ名を厳密に照合**するため（`"Cubic.easeOut"` が正）、関数が見つからず次のフレームで例外。Phaserのゲームループは例外が出ると**次のフレームを予約しない**ため、その時点でゲーム全体が停止する
- Tweenの方は同じ表記でも内部で補正してくれるので、気づきにくかった
- 対処: 4か所を `"Cubic.easeOut"` に修正。ヘッドレスブラウザで「練習開始→撃破→ESC→ホームに戻る」がタイトルへ戻ることを確認
- 付随: オンライン退出時 `disconnect()` が `closed` を発火して `leave()` が二重に呼ばれ、タイトルのメッセージが「接続が切れました」に上書きされていた。`leaving` フラグで一度だけにした
- 裁定29の「固まる」報告の一部はこの不具合だった可能性がある

## グラウンドスラムの演出（裁定33・ユーザー決定）
- 溜め中のリングを「縁から中心へ縮む」→「**中心から外へ広がる**」に反転（溜めが満ちるほど範囲が埋まる見え方）
- 発動時に**中心から範囲の縁まで一気に広がる衝撃波**（橙のリング＋薄い塗り＋白フラッシュ）を追加。従来は発動の瞬間に何も出ていなかった

## ビルドウォールの持続時間（裁定34・ユーザー決定）
- `wall.seconds` 4.5 → **6.5**（+2秒）。テストの固定値 2.5 は `balance.ts` 参照に置き換えた

## 訓練場の仕様（裁定35・ユーザー決定）
- 裁定14で「時間の概念を持たせない」と決めていたが、**sim側では2分タイマーが動き続けて時間切れで試合が終わっていた**（仕様と実装の食い違い＝バグ）
- `SimState.practice` フラグを追加。訓練場では **時間を進めない／残機を減らさない／撃破後は通常の復活時間（`respawnSeconds`）で自動復活／試合は終わらない**
- ESCメニューに「**キャラ変更（今: ○○）**」を追加。スピード→タンク→サポートの順に回し、リセットと同じ初期状態で入り直す。相手（的・bot）はそのまま
- 通常戦への影響なし（フラグが立っていないときは従来どおり）。テスト3本追加（時間切れなし／残機不変と自動復活／通常戦は従来どおり終わる）

## 移動速度（裁定36・ユーザー決定）
- 横断秒: スピード 2.1 → **2.4** / タンク 2.5 → **2.8** / サポート 2.5 → **2.8**（全員遅く。速度比はスピード:他 = 1.17倍で、旧1.19倍からほぼ据え置き）
- 影響: ソニックの距離やスキルリンクの成立範囲は画面幅比で定義しているので変わらない。ブリーチが繋がりにくい懸念（未解決2）は移動が遅くなったぶん、わずかに繋がりやすくなる方向

## 効果音の穴埋め3点（裁定37・ユーザー決定）2026-08-25
背景: ドパガキ視点の棚卸しで、(1) 被弾側が無音、(2) 全武器のヒット音が同じ、(3) 撃破音が誰の撃破でも同じ、の3つを構造的な穴として指摘し、まとめて埋めた。
- **被弾音 `hurt`**: 自分が殴られたときに鈍く低い「ドスッ」。中心を取られたときは短い不協和音を重ねる。攻撃側の音（明るく高い）と対照にして、聞くだけで「殴った／殴られた」が分かるようにした
- **武器別ヒット音 `hit(weapon, combo, center)`**: 刀=鋭い斬撃＋金属の余韻／ピストル=乾いたスナップ／HMG=短く重い（連射なので控えめ）／ナイフ=低く湿った刺突／狙撃=長い尾を引く一撃／ジャブ=軽い「ポッ」。連続ヒットのピッチ上昇は全武器共通で維持。中心ヒットは武器音の上に高い抜け音を重ねる方式に変更（以前は武器音を置き換えていた）
- **撃破音の3分岐 `kill("mine"|"me"|"other")`**: 自分が倒した=爆発＋上昇アルペジオ（C6→E6→G6→C7、撃破スロー0.3秒内に鳴り切る）／自分が倒された=低く沈む下降音／他人同士=従来の爆発を小さく遠くで
- sim側: `hit` イベントに `weapon: HitWeapon` を追加（近接はクラスで一意、弾は BulletKind、リンク効果のスタンは "link"）。テスト1本追加
- 次候補（未着手・優先順）: A ゲージ満タン通知 → B 死にスティング＋復活音 → C 多段キル → D 低HP心音（ON/OFF付き） → E トドメの一撃の音

## バレットプルーフとポーションの見える化・ポーション射程（裁定38・ユーザー決定）2026-08-25
背景: 「バレットプルーフが発動していない気がする」との報告。調査の結果、発動しても自機が薄く点滅するだけで専用の音も演出もなく、ポーションも範囲が見えないため、効いているかどうかが判別できない状態だった。
- **バレットプルーフ**: 発動の瞬間に水色のリングが弾けて広がり「PROOF」のポップ＋専用音（ガラスが張るような高い「キィン」）。無敵中は**水色の六角シールド**が対象を取り囲み、残り時間で薄くなる。味方に投げて当たった場合も同じ演出。表示用に `bulletproofT` を追加（無敵の実体は従来どおり `invuln`。攻撃で無敵が解除されたら表示も消える）
- **ポーション**: 構え中に**着弾予定地点へ投擲線と回復範囲の円**を表示（ビルドウォールのプレビューと同じ方式）。飛んでいる間も着弾点に範囲を出し続け、炸裂時は範囲いっぱいに緑の円が広がる＋専用音。回復した相手がいなくても炸裂演出は出す（`potion` イベント）
- **ポーションの射程**: `throwMaxPlayers` 8 → **16**（2倍）
- 調査で見つかった**未決の2点**（ユーザー判断待ち）:
  1. 単押し／長押しの境目が0.15秒と短く、普通に押しただけで長押し扱いになる。味方がいない場面（訓練場・乱闘）ではそのまま**不発**になる（検証: 0.1秒押し→無敵0.75秒、0.2秒押し→無敵0）。提案: 投げる相手がいなければ自分に使う＋境目を0.25秒へ
  2. 無敵がリスポーン無敵と同じフィールドのため、**発動直後に攻撃すると即解除**される（初期に「調整枠」として記録済み）。提案: 専用の無敵にして攻撃で消えないようにするか、無敵中は攻撃不可にするかの二択

## 訓練場のキャラ変更を直接指定に（裁定39・ユーザー決定）2026-08-25
- 裁定35の「順に回す」方式をやめ、ESCメニューに**今のキャラ以外の2つ**を「○○に変更」として並べる。1回の操作で目当てのキャラに入れる
- 挙動はそのまま（リセットと同じ初期状態で入り直す。相手は的／botのまま）

## タッチ操作・段階1（裁定40・ユーザー決定）2026-08-25
方式は **A: Brawl Stars式**（ユーザー選択）。実装は `client/src/touch.ts`（仮想スティックとボタン）＋ `GameScene.readTouchInput`（タッチ→PlayerInput の翻訳）。
- **切替**: タイトル右上「操作: タッチ／キーボード」。localStorage に保存。未設定ならタッチ端末（`pointer: coarse` かつタッチ点あり）で自動的にタッチ
- **スティック**: 画面左45%のどこを触ってもそこに出る浮遊スティック。アナログ移動
- **ボタン**（右下）: 主武器（大）・副武器・防御・スキル1〜3。タップ＝一番近い敵へ**オートエイム**で即発動／ドラッグ＝手動照準／離す＝発射／ボタンの上へ戻して離す＝キャンセル（`PlayerInput.cancel` を追加し、右クリックと同じ扱い）
- **武器ごとの発火タイミング**: 近接（刀・ナイフ・ジャブ）は離した瞬間に1回（向きを決めてから振れる）。射撃（ピストル・HMG）は押している間。狙撃はドラッグ中が溜め、離すと発射
- **スキルの発火タイミング**: 構え型（ウォール・ポーション）は押した瞬間に構え、離すと設置／投擲。距離はドラッグ量に比例（タップならウォールは2.5体分前方、ポーションは足元）。それ以外（ソニック・クラウド・チャージ・スラム・かばう・スタン弾）は離した瞬間にドラッグの向きで発動
- **バレットプルーフ**: sim側の「押した長さ」判定を使わず、**ドラッグせず離した＝自分／ドラッグした＝その方向の味方へ投擲**とジェスチャーで決める（タッチでは押す長さが安定しないため）
- **HUD**: 下のキー案内は消し、ボタンに武器名とCD（暗い扇）・ゲージ不足（薄く）を表示。ESCの代わりに右上「メニュー」ボタン。自機の向きを短い線で表示（ドラッグ中は太く長く）
- **画面**: 横向き専用。縦持ち時はCSSで「横向きにしてください」を表示。ピンチズーム・スクロール・長押し選択を無効化
- **確認**: タッチをエミュレートしたブラウザで、移動・刀（離して振る）・ピストルのドラッグ照準・ウォールのドラッグ設置とキャンセル・ポーション投擲・プルーフのタップ・狙撃の溜め→発射が通ることを確認
- **段階2以降（未着手）**: ルームコードのテンキー入力、ロビーのタッチ対応、スマホがホストのときの警告、ボタン配置と大きさの調整（右側のボタンは敵と重なる位置に来るので透過度と配置は調整枠）、ドラッグ感度

## スマホで画面の上下が切れる不具合の修正（裁定41）2026-08-25
実機（iOS Safari 横持ち）で、上のブラウザバーの分だけ画面がずれ、**下のボタン列が見えなくなる**不具合を修正。
- **原因**: `#game` の高さが `100vh`。iOS Safari の `100vh` は上下のブラウザバーに隠れる領域も含んだ高さのため、FIT スケールのキャンバスが実表示領域より縦に大きくなり、中央寄せの結果として上下がはみ出して切れていた
- **対処**: 実際に見えている高さ（`window.visualViewport.height`、無い環境では `window.innerHeight`）を CSS 変数 `--vvh` に入れ、`#game` の高さに使う。`resize` / `orientationchange` / `visualViewport` の `resize`・`scroll` で追従し、そのつど `game.scale.refresh()` を呼ぶ
- **併せて**: `viewport-fit=cover` でノッチ下に潜り込まないよう、`#game` に `env(safe-area-inset-*)` のパディングを追加
- **確認**: 端末に近い横長の表示領域（844×320・タッチ有効）でキャンバスが表示領域にぴったり収まり、はみ出しゼロ・エラーなしを確認

## 「剣」への改名と近接の当たり判定修正（裁定42）2026-08-25
- **改名**: 画面上の表記を **刀 → 剣** に統一（ボタン・タッチボタン・キャラ説明・SPEC）。内部の識別子は `saber` のまま（過去の裁定・テストとの整合を保つため）
- **当たり判定バグ**: 症状は裁定23と同じ「刃先に判定がない」。裁定23で直したのは**角度**だけで、**距離**側に別のズレが残っていた
  - 原因: 描画は刃を「自分の中心から `reach + 自分の半径`」まで伸ばしているのに、判定は「**相手の中心**がその距離内」を要求していた。相手の半径が足されておらず、刃先が相手の体に重なっていても中心に届かなければ当たらない。半径24・リーチ74なので、見えている刃の**先端2割強**が空振りになっていた
  - 対処: 上限を `reach + 半径 * 2`（刃の長さ＋相手の半径）に変更。これで「体が刃に触れていれば当たる」という見た目どおりの挙動になる
  - 影響: 剣・ナイフ・素手の3つすべて。実効リーチが約24px（キャラ1体分）伸びるので、近接の間合いが体感で広がる。強すぎるようなら `reach` 側を下げて調整する
- **確認**: 中心間 `reach+半径*2` の内側で当たり、外側で当たらないことをテストで固定。テスト79本通過

## 集団戦の敵味方の見分け（裁定43・ユーザー決定）2026-08-25
症状: 3v3で敵味方が判別しづらい。原因は、敵味方を区別しているのが**キャラの輪郭線3pxだけ**だったこと（体の塗りはクラス色なので敵味方で同じ）。加えて**弾に敵味方の区別が一切なかった**（武器ごとの色のみ）。
採用したのは A・B・C の3点（ユーザー選択）。
- **A: 足元のチームリング** — キャラの真下にチーム色の楕円（塗り0.22＋線0.75）。体の塗りを邪魔せず、視界の端でも拾える大きな色面を作る
- **B: 敵弾の赤い縁取り** — 武器ごとの色はそのまま残し、敵チームの弾にだけ赤を足す。通常弾は外周のにじみ＋芯線を赤＋外周円、投擲フラスコは縁を白→赤に。「赤い縁＝避ける」という単純な規則にした
- **C: 名前の文字色をチーム色に** — 全員グレーだったものを味方 `#93c5fd` / 敵 `#fca5a5` に
- **HPバー** — 味方（自分含む）=緑 / 敵=赤。以前は**自分だけ緑で味方は敵と同じ赤**だったため、これが混乱の主因だった
- **【Claude提案・不採用】敵のクラス色を落として味方を明るくする** — 敵こそ最も見えるべき対象であり、暗くすると背景に沈むため推奨せず、選択肢からも外した
- **確認**: 実ブラウザで自機（青リング・青名前・緑HP）と敵（赤リング・赤名前・赤HP）の描き分けを目視確認。エラーなし・テスト79本通過

## ロビーのbot設定を個別化（裁定44）2026-08-25
これまでは「botのLv/キャラ」1つのボタンで**最後に追加したbotだけ**を Lv1→Lv2→Lv3→次キャラのLv1… と一列に回す方式で、途中のbotは設定を変えられなかった。
- **対象の選択**: 「対象: CPU1」ボタンでbotを順に選ぶ。参加者一覧の該当行に `▶` を表示して、いまどれを編集しているかが分かるようにした
- **Lv**: 「Lv: 2」ボタンで 1→2→3→1 と切り替え（キャラは変わらない）
- **キャラ**: 「キャラ: タンク」ボタンでスピード→タンク→サポートと切り替え（Lvは変わらない）
- bot追加時は**追加したbotが自動で編集対象**になる。削除時は対象の添字を詰める
- botが0体なら3つとも無効表示、1体だけなら「対象」ボタンのみ無効
- **レイアウト修正**: bot操作を上段（追加・削除・チーム分け入替・モード）と下段（対象・Lv・キャラ）の2段に整理し、開始／退出を最下段へ移動。以前の配置では**開始・退出ボタンがbot操作ボタンに重なっていた**
- **確認**: ローカル中継サーバーを立てて実ブラウザでロビーに入り、bot3体追加→対象をCPU3からCPU1へ切替→CPU1だけがタンク・Lv3に変わり他2体は変化なし、を目視確認。エラーなし

## 中央エリアと移動速度半減（裁定45・ユーザー決定）2026-08-25
3v3が個別戦闘になる問題への対処。Claude提案4案（案1 中央の取り合い／案2 味方が近いと強化／案3 敵陣で強化／案4 残機のチーム化）のうち、**案1のみ**をユーザーが採用。形は「広めの四角」。
- **中央エリア**: フィールド中央に幅42%×高さ55%の長方形。**チーム戦のみ**（乱闘・訓練場には出ない）
- **ルール**: エリア内の生存人数で**単独で勝っている**チームのゲージだけが溜まる。同数・無人は誰も溜まらない。満タン（`captureSeconds: 8`・調整枠）で**相手チームの残機-1**、ゲージは0に戻って溜め直し。減衰なし
- **表示**: 四角の塗りと線を「いま人数で勝っている側」の色（味方=青／敵=赤／拮抗・無人=灰）にして脈動。上辺の上に青・赤のゲージを左右に並べる（中央で出会う向き）。制圧時は通知・ポップ・リング・撃破音・軽い揺れ
- **【Claude提案・不採用】案2〜4** — ユーザー判断で見送り。再提出しない
- **【ユーザー案・不採用】自陣で敵の移動速度1/2** — 押し込む側を弱くするので両者が自陣に引きこもり膠着する、という指摘をユーザーが受け入れて案1へ
- **移動速度**: 全員の移動速度に `moveSpeedMultiplier: 0.5` をかける（ユーザー指示・試行）。横断はスピード4.8秒／タンク・サポート5.6秒。**弾速は据え置きなので回避が難しくなり、射撃寄りに傾く**副作用あり。戻すときは倍率を1にするだけ。キャラ解説の横断秒は `crossSecondsOf` で倍率込みの実値を表示
- **既知の弱点**: botはエリアを認識しない。人間vsbotのチーム戦では人間側が一方的に取れる
- **確認**: テスト83本（エリア4本追加: 生成条件／単独優位で溜まって満タンで残機-1／同数は溜まらない／減衰なし）。実ブラウザで2v2を組み、四角・ゲージ・色の切替を目視確認

## テンポ調整とリンク易化（裁定46・ユーザー決定）2026-08-25
「動きが早すぎて展開についていけない」「戦闘がバラバラで対面にしか意識を割けない」というユーザー体感への対処。
- **移動速度**: 倍率 0.5 → **1/3**（さらに2/3）。横断はスピード7.2秒／タンク・サポート8.4秒。弾速・攻撃テンポは据え置きなので射撃有利に傾く副作用は継続（裁定45の指摘どおり）
- **ポーション弾速**: 300 → **600**（2倍）
- **設置型の持続2倍（ビルドウォール除く）**: クラウド 2 → **4秒**。グラウンドスラムのリンク受付枠をスタン時間（0.5秒）から切り離して `linkWindowSeconds: 1.0` に（スタン時間そのものは据え置き）。ウォールはユーザー指示で除外（6.5秒のまま）
- **リンクの時間窓 0.5 → 2.0秒**（Claude追加）: 設置型の持続を伸ばしても、**相方のスキルが「0.5秒以内」でないと成立しない**判定が残るとリンクは楽にならない。持続時間はクラウド内に弾を撃ち込む余裕、時間窓は相方が反応する余裕で、易化には両方が要る。戻すなら `link.windowSeconds`
- **確認**: テスト84本（クラウド持続テストを balance 追従に修正、「1.5秒あいてもリンク成立」を1本追加）
- **未対応**: リンク成立の分かりづらさ（裁定30で中央テキストを廃止した経緯があるため、案を提示して判断待ち）

## スキルリンクの成立条件を「オブジェクトが残っている限り」に（裁定47・ユーザー決定）2026-08-25
- **成立条件**: 時間窓（裁定46で0.5→2.0秒にしたもの）を**廃止**。先に出したスキルの**オブジェクト（ウォール・クラウド・スタン弾）が残っている限り**、相方のスキルで成立する。ウォールなら6.5秒、クラウドなら4秒、スタン弾なら飛んでいる間
- **例外**: 残るオブジェクトを持たないスキル（ソニック）が**先**に出た場合だけ、従来どおり0.5秒の猶予（`link.objectlessWindowSeconds`）。「ソニック→直後にウォール」を拾うため
- **距離判定**: 「使った人の位置」から「**オブジェクトの位置**」基準に変更。ウォールは5体分先に置けるので、置いた人からの距離では意味がなかった
- **スラム系**（スラム×スタン弾／スラム×ポーション）は元からスラムの受付枠（裁定46で1.0秒）基準なので、扱いは同じ
- **リスク（指摘済み）**: ウォール×ソニックは6.5秒間ほぼ自動成立になる。強すぎればウォールの持続かリンク距離（`link.maxDistanceRatio`）で絞る
- **エフェクト（ユーザー案）**: 成立したオブジェクトを**明るい緑で縁取り**（二重線＋白い芯＋周囲を回るきらめきの点、2.2秒で減衰）。**敵のリンクはマゼンタ**で同じ縁取りにして見分ける。スラム系はオブジェクトが残らないので成立地点に円で縁取る
- **音（ユーザー案）**: 鈴のようなアルペジオ。**味方は上昇**（G5→B6）、**敵は下降**。余韻の低音も味方G3／敵E3で分ける（`SFX.linkSparkle(mine)`）
- **【Claude提案・不採用】画面の縁フラッシュ／技名の現場表示／短い停止／当事者だけ音を大きく** — ユーザーが縁取り＋キラキラ音を選択したため見送り
- `link` イベントに `team` と `object: {kind, id} | null` を追加
- **確認**: テスト86本（「ウォール5秒後のソニックでブリーチ成立」「クラウド消滅後のスタン弾は不成立」を追加）。実ブラウザで味方クラウド＝緑／敵ウォール＝マゼンタの縁取りを目視確認
- **ユーザー確認（裁定47補足）**: (1) ウォールが残っている間は**ソニック側次第で合わせられる**で良い（0.5秒の猶予は同時押しの取りこぼし防止として残す）。(2) 距離をオブジェクト基準にするのはOK。(3) ウォール×ソニックがほぼ自動成立になる件は「壁を越えられるだけ」なので問題なし → 調整不要と判断

## botを名前のタッチで選ぶ（裁定48・ユーザー決定）2026-08-25
裁定44では「対象」ボタンを押して順送りしていたが、botが増えるほど目当てのbotまで何度も押す必要があった。
- 参加者一覧を**1つのテキストから行ごとのテキストに分割**し、bot行をタッチ／クリックで直接選べるようにした
- 押せるbot行には `・`、選択中には `▶` を付け、選択中はシアン、その他は薄いグレーで表示。マウスではホバーで色が変わる
- 当たり判定は行の高さ（30px）ぶんを左右に20pxずつ広げて確保し、指でも押しやすくした
- 「対象」ボタンは不要になったので**削除**。代わりに「編集中: CPU1（名前を押して変更）」の案内ラベルを置き、Lv とキャラのボタンだけ残した
- bot行以外（人間プレイヤー）は押せない
- **確認**: 実タッチイベント（CDP `Input.dispatchTouchEvent`）でbot3体を追加し、CPU1の行をタップ→選択がCPU3からCPU1へ移動、続けてLv・キャラを変更してCPU1だけが変わることを確認。エラーなし

## ボス戦モード（裁定49・ユーザー決定）2026-08-25
「倒す敵が1人ならわかりやすい」というユーザー提案。**teams の特殊形**として実装した（チーム残機・HUD・勝敗判定・スポーンを再利用できるため。新モードを独立させると全分岐に手が入り壊れやすい）。
- **構成**: 4人ちょうどで、一覧の**最後の1人（bot）がボス**。挑戦者3人がチーム0、ボスがチーム1。ロビーのモードは4人時に「乱闘 → 2vs2 → ボス戦」の順で回る。ボスがbotでないときはボス戦を選べない
- **ボスの強さ（2-A: HP方式をユーザー選択）**: HP **6倍（600）**・与ダメ **1.4倍**・クラスの被ダメ補正を受けない・引かない（`retreat` 無効）・中央から開始
- **狙いの切り替え（ユーザー案）**: **4秒ごとに「最も近い人」か「最もダメージを出している人」をランダムで引き直す**。基準が固定だと対策も固定化するため、どちらを見ているか読ませないのが狙い
- **範囲ノックバック（ユーザー案・新スキル）**: 半径260内に**2人以上**入ったら自動で溜め開始 → **0.5秒**の溜め（円が縮む予告つき）→ 全方向へ吹き飛ばし（距離300・近いほど強い）＋0.4秒硬直＋8ダメ。CD7秒。ボスは入力を持たない（bot専用）ので**シミュレーション側で自動発動**する。囲まれ問題を「AIの賢さ」ではなく技として仕組みで解決する形
- **勝敗（3・ユーザー承認）**: ボスのHPを0にすれば挑戦者の勝ち。挑戦者は**共有残機7**が尽きたら負け。**中央エリアは出さない**（3対1で人数優位が意味をなさないため）
- **半径は変えない**: ボスを大きくする案は取り下げ。当たり判定は23か所に影響し、見た目だけ大きくすると**裁定42で直した「見えているのに当たらない」が再発する**
- **HUD**: ボスHPを画面上部に大きく表示（頭上の小バーでは6倍のHPの残量が読めないため）。挑戦者側は「残機 ◆×7」表示
- **併せて修正した既存バグ**: 味方判定が `meState ? ... : mine` の形で、自分の状態が取れない瞬間に**自分以外が全員敵扱い**になっていた（キャラの色・名前の色の両方）。`mine || (...)` に修正
- **リスク（指摘済み・ユーザー了承）**: 2-Aは「削り切るゲーム」になり、押し続ける動機は生まれない。間延びしたら回復か段階制を足す
- **確認**: テスト91本（ボス生成・中央開始・勝敗・与ダメ倍率・ノックバックの溜めと発動とCD・ボス戦以外では起きないこと）。実ブラウザでロビーからボス戦を開始し、HP600・残機7・エリアなしとHUDの表示を目視確認

## ビルド印（裁定50・ユーザー決定）2026-08-26
### 背景（実際に起きた事故）
タッチ操作一式（裁定40〜）が「無かったことに」なっていた件の原因:
- **GitHubの「Upload files」は同じパスを無条件で上書きする。**新旧を判定しないので、古いzipを1回上げるだけで前の変更が静かに消える
- zipが毎回ダウンロードフォルダを経由するため、同名ファイル（`dopagaki-arena__2_.zip` のような重複名）から古いほうを選ぶ事故が起きる
- 同じ事故がチャット側でも起きた。裁定32時点の古いzipを土台に作業してしまい、裁定33〜49の17件が抜けた状態で修正を作った（このzipは破棄）

### 対応
- タイトル画面の右下に **`裁定NN / YYYY-MM-DD`** を小さく表示。デプロイ後にShift+リロードして番号を見れば、古い版が上がったかどうかが一目で分かる
- **値は手書きしない。**`client/vite.config.ts` が `docs/DECISIONS.md` を読んで最新の裁定番号を自動抽出し、`__BUILD_ID__` として埋め込む。手動更新は忘れるため
- 日付はビルド時刻を日本時間に直したもの（Cloudflareのビルド環境はUTC）
- `docs` が無い環境でもビルドは通す（印が `裁定?` になるだけ）

### 運用ルール
- 上げたあと右下を見る。**番号が進んでいれば成功、戻っていたら古いzipを上げた事故**
- 新チャットの冒頭では、**必ず最新zipを添付し、Claudeは裁定番号を照合してから作業を始める**
- 限界: 「新しいzipを上げ忘れた」場合も番号は変わらないので、症状は同じに見える。どちらも「反映されていない」と判断すればよい

## エラーの可視化と、訓練場メニュー中の停止（裁定51）2026-08-26
### 背景
Phaserは**フレーム処理で例外が外へ漏れると次フレームを予約せず、描画ループが永久に止まる。**画面には最後に描いた絵が残るため「ボタンを押した瞬間に固まった」ようにしか見えず、非エンジニアには原因が分からない。裁定29の「撃破時に止まる」、shutdownでカメラに触って遷移が止まった件も同じ機構。

### 対応
- **エラー表示オーバーレイ**（`client/src/errors.ts`）。`window.onerror` と `unhandledrejection` を捕捉し、エラー名・メッセージ・スタック上位3行を画面下部に赤枠で表示。**「再読み込みしてホームに戻る」ボタン**を必ず出すので、固まっても抜け出せる
- **`GameScene.update()` を try/catch で包囲**し、本体を `tick()` に切り出した。1フレームぶんの例外でゲーム全体がブリックしなくなる
- 方針: エラーは握りつぶさず**必ず画面に出す**。黙って続行すると原因が隠れるため

### 訓練場のメニュー中は時間を止める
- 従来はメニューを開いてもシミュレーションが進み続け、**メニューを読んでいる間にbotに殴られる／試合が終わって結果画面に飛ばされる**状態だった
- 訓練場（`session.mode === "solo"`）のみ、メニューが開いている間 `tick()` を停止
- **対戦中は止めない**（相手が動いているのでオンラインは同期が崩れる）

## 移動速度を3.0／4.0／4.0秒へ（裁定52・ユーザー決定）2026-08-26
- 画面横断: スピード **3.0秒** / タンク **4.0秒** / 支援 **4.0秒**（ユーザーが実プレイ後に指定）
- **裁定45（×0.5）・裁定46（×1/3）の減速を撤回する方向の変更。**実効値は 7.2/8.4/8.4秒 → 3.0/4.0/4.0秒 で、**約2.3倍速くなる**
- 構造: `moveSpeedMultiplier` を **1** に戻し、横断秒を `crossSeconds` に直接書く方式へ統一。倍率と実値の二重管理をやめた（`crossSecondsOf` は倍率込みなので表示は自動で追従）
- タンクと支援を同値にしたため、速度差は「**スピードだけが速い**」構図に変わった（比 1.17倍→**1.33倍**）
- 【Claude指摘・要観察】
  - 裁定45・46は「3v3が個別戦闘になる」「テンポ」への対処として**意図的に減速したもの**。それを2.3倍に戻すので、**個別戦闘化が再発する可能性**がある。中央エリア（裁定45）が効いているかを見てほしい
  - 弾速は据え置きなので、**相対的に弾が避けやすくなり、射撃有利の傾き（裁定45・46の副作用）は解消方向**
  - **ソニックの相対価値は下がる**（通常移動が速いぶんダッシュのありがたみが薄れる）
  - スキルリンクの成立距離・ビルドウォールの設置距離が、体感では「近く」なる

## ボス戦の作り直し（裁定53・ユーザー決定）2026-08-26
実プレイで「ボスがすぐ負ける」「ボスっぽくない」となったため4点変更。原因はHP不足だけでなく、**裁定49のボスが専用の攻撃を持たず、ロビーで設定したbotのクラスの武器をそのまま使っていた**こと（HPと与ダメだけ違う普通のキャラだった）。

### 扇状射撃（新スキル・ユーザー選択）
- CDが明けたら**狙った方向へ5発を扇状に撒く**。端から端まで44度なので、**隙間を抜けて避けられる**
- 低ダメージ（6）・低速（430）・大きめの弾（半径11）。「弾除けが楽しい」を成立させるための組み合わせ
- **通常弾扱いなので剣で消せる。**距離減衰を受けないよう `hmg` 種別を使う
- ボスは入力を持たない（bot専用）ので、ノックバックと同じく**シミュレーション側で自動発動**
- のけぞり中とノックバックの溜め中は撃たない（溜めの予告を潰さないため）

### 体を大きくする（裁定49の「大きくしない」を撤回）
- `radiusMultiplier: 1.6`。**裁定49で見送ったのは「見た目だけ大きくすると裁定42の空振りが再発する」ため**であって、大きくすること自体が悪かったわけではない
- 今回は `radiusOf()` を新設し、**当たり判定・壁の押し出し・場外クランプ・近接の見込み角・弾の命中・描画・名前の表示位置**をすべてこれ経由に統一した。**片方だけ P.radius のまま残すと空振りが再発する**
- ビルドウォールの設置距離とポーションの投擲距離は `P.radius * 2` のまま。ここは「キャラ何体分」という**物差し**であって体の大きさではないため
- **副作用: 的が大きい＝当てやすい＝実質HPが減る。**hpMultiplier と相殺関係にある

### 数値
- **HP倍率 6 → 10**（600 → 1000）。裁定52で挑戦者が2.3倍速くなったぶんの上乗せも兼ねる
- **与ダメ倍率 1.4 → 1.0**（ユーザー指示「攻撃力低め」）。近接で殴り倒す形から、弾を撒く形へ寄せた
- **近接は残す**（ユーザー選択）。近づかれたら殴れる

### 【Claude指摘・未対応】
- **ノックバックが空振りする可能性は残っている。**半径260・溜め0.5秒は裁定52の減速時に決めた値なので、2.3倍速くなった今は見てから逃げ切れるかもしれない。実プレイで空振りが続くようなら `windupSeconds` を縮めるか `radius` を広げる
- 2-A（HPで強さを表す方式）が「削り切るゲーム」になるリスクは裁定49から継続。HP1000でさらに間延びしやすくなったので、退屈なら回復や段階制を足す

### 確認
テスト94本（扇の発数・広がり角・通常弾判定・低ダメージ・CD・ボス戦以外では起きないこと・ボス半径・**普通のキャラの半径より外／ボスの半径より内を通る弾が当たること**の3本を追加）。型チェック・本番ビルド通過。

## ロビーのモード選択を縦4ボタンに（裁定54・ユーザー決定）2026-08-26
従来は「モード」1個のボタンを押して 乱闘→2vs2→ボス戦 と順ぐりに切り替える方式だった（裁定49）。**目当てのモードに行くのに何回押せばいいか分からず、今どれが選ばれているかも読みにくい**ため、直接選ぶ形に変更。

- 右側に**縦4つ**を並べる。順は上から **乱闘／ボス戦／2vs2／3v3**（ユーザー指定）
- **押せば即そのモード。**順ぐりのトグル（`toggleMode`）は廃止

### 内部構造（重要）
- **2vs2 と 3v3 は、内部的にはどちらも `teams` で、人数で決まる。**別モードではない
- そのため画面用に `ModeChoice = "ffa" | "duo" | "trio" | "boss"` を新設し、**UIだけ4値・通信する `MatchMode` は従来どおり3値**にした。ロビーの通信仕様は変えていないので、旧クライアントとの食い違いは起きない
- 成立条件: 乱闘=2〜4人／2vs2=4人ちょうど／3v3=6人ちょうど／ボス戦=4人ちょうどで一覧の最後がbot
- 人数が変わって今のモードが成立しなくなったら**自動で乱闘に戻す**（成立しないモードのまま開始できてしまうのを防ぐ）

### 見せ方
- `ui.ts` の `button()` に **`setSelected()` を追加**。「選択中」を黄色の枠と文字で表す。**「押せる／押せない」とは別軸**にして、選択中は押せない状態より優先して見せる（選ばれているのに灰色、を防ぐため）
- ホスト以外にも4つを表示する（押せないが、**いま何が選ばれているかは全員が見える**）
- 押せない理由は**ボタン名ではなく下の小さい注記**に出す。ボタン名を長くすると枠からはみ出すため

### 確認
テスト94本・型チェック・本番ビルド通過。ロビーはシミュレーション外なので自動テストの対象外。**実ブラウザで、4人・6人・botなしの各状態でボタンの活性と選択表示を目視確認すること。**

## モードの説明ポップアップ（裁定55・ユーザー決定）2026-08-26
裁定54で4つ並べたが、**名前だけでは何が起きるゲームか分からず、押せない理由も読めない**ため、カーソルを合わせたら説明が出るようにした。

- ボタンの**左隣**に、背景つきのパネルで表示（右側は画面外なので必ず左へ開く）。上下は画面からはみ出さないよう寄せる
- 中身は3段: **モード名 → 何をするゲームか → 人数条件と、今の顔ぶれで何が足りないか**
- **押せる／押せないに関係なく出す。**押せない理由を読むためのものでもあるため
- パネルは1つだけ作って中身を差し替える（ホバーのたびに作り直さない）

### 人数の案内は状況に応じて文を変える
`modeNeedText()` が今の参加者から計算する。**人間は増やせないので、足りないぶんは必ずbotを足す案内にする。**
- 例: `条件: 6人ちょうど` / `いま4人（プレイヤー1・bot3）` / `→ botをあと2体追加`
- 多すぎる場合は `→ ◯人減らす`
- ボス戦だけの特例: **人数は足りているのに一覧のいちばん下がプレイヤー**のときは、`「チーム分け入替」で下をbotにする` と具体的な操作を出す。ここは条件が分かりにくく、詰まりやすいため

### 【Claude指摘・限界】
- **スマホではホバーがない。**タッチだと押している間だけ出て、指を離すと消える（Phaserはタッチを離すと pointerout を出すため）。長押しで読む形になる。専用のタッチUIが要るなら別途

## 画面いっぱいに広げ、UIをフィールドの外へ（裁定56・ユーザー要望）2026-08-27
### 原因
キャンバスを **1280x720 固定**にして FIT で拡大していたため、16:9 より横長な端末（**ほとんどのスマホの横持ち**）では左右に黒帯が出ていた。**この黒帯はキャンバスの外なので、触っても一切イベントが来ない。**「フィールド外をタッチしても反応がない」はこれ。

### 対応
- **キャンバスの横幅を端末の比率に合わせて広げる**（`client/src/viewport.ts` の `VIEW`）。高さは720固定、上限は21:9
- **プレイフィールド 1280x720 の大きさは変えない。**各シーンの `create()` で `applyView()` を呼び、カメラを `-offsetX` だけずらしてフィールドを中央に置く。**各シーンは今までどおり 0..1280 / 0..720 で組めばよい**
- **見える範囲は広がらないので、端末の比率で有利・不利は生まれない。**広がるのは「画面」だけで、フィールドの外は余白

### フィールドの外へ出したもの
- **タッチのボタン6つ**。画面の右下隅からの相対位置に変更し、毎フレーム `relayout()` で追従。**横長な端末ほど余白側へ出ていき、指がフィールドに被らなくなる**
- **移動スティックの判定を画面全体の左45%に拡大**（従来はフィールドの左45%）。**左の余白を触っても動かせる**
- **右上の「メニュー」ボタン**を画面の右端へ
- ESCメニューの暗転と背面ブロッカーも余白ぶんまで拡張。**フィールドの外を触ってメニュー越しに操作できてしまうのを防ぐ**
- カメラの揺れは「中央に置いた位置」からの相対に修正（`setScroll(0,0)` に戻すとフィールドが左へ寄るため）

### 画面サイズの変化
`recomputeView()` で測り直し、`setGameSize()` と各シーンのカメラずらしを更新する。タッチボタンは毎フレーム再配置するので自動で追従する。

### 【Claude指摘・未確認】
- **実機での目視確認が必須。**シミュレーション外なので自動テストが効かない。特に「フィールドの左右に余白が出るか」「ボタンが余白側に出ているか」「左の余白でスティックが出るか」の3点
- 縦長（16:9より縦に長い）画面では従来どおり上下に黒帯が出る。横向きを促しているのでスマホでは実質起きない
- デスクトップの16:10などは16:9より横に狭いため余白は出ない（従来と同じ見え方）

## ルーム設定の保持（裁定57・ユーザー要望）2026-08-27
ルームを作り直すたびにbotを並べ直すのが手間なため、前回の卓の形をブラウザに覚えさせる。

- **明示的な「保存」操作は作らない。**ホストが卓をいじるたび（bot追加・削除・Lv・キャラ・モード・入替）に自動保存する。プリセット名を付けて管理する形も検討したが、**まず1つ覚えるだけで手間の大半が消える**ため、複数プリセットはv2送り
- 保存先は `localStorage`（`dopagaki.room.v1`）。`client/src/roomPrefs.ts`
- 保存するのは **bot構成・モード・並び順ローテーション・自分のキャラ**だけ。**ルームコードと参加者は毎回変わるので保存しない**
- bot構成とモードは**ホストのときだけ復元**する（ゲストが復元するとホストの卓と食い違う）。自分のキャラは誰でも復元する

### 読み込みは必ず検証する
古い版の保存や壊れたJSONが残っていても落ちないよう、`loadRoomPrefs()` で全項目を検証する。
- Lvは1〜3以外なら2、キャラ・モードは既定値へ、bot数は5体まで
- **botのidとnameは保存値を使わず必ず作り直す**（重複IDのbotが生まれるのを防ぐため）
- 読み書きが例外を投げても握りつぶす（プライベートモードや容量超過で遊べなくなるのを防ぐ）

## 対戦終了後に「ルームに戻る」（裁定58・ユーザー要望）2026-08-27
結果画面から卓を組み直せなかった（再戦は同じ構成、ホームに戻ると接続が切れる）ため、中間の導線を足した。

- 結果画面のボタンを**3つ**に: `再戦 / ルームに戻る / ホームに戻る`
- ソロにはルームが無いので、ラベルを **「キャラ選択へ」** に変えてキャラ選択画面へ戻す
- **オンラインではホストだけが押せる。**ゲストが勝手にロビーへ抜けるとホストの再戦に乗れなくなるため
- **ゲストはホストに追従する。**ホストがロビーに入ると `lobby` メッセージが飛ぶので、結果画面でそれを合図に一緒にロビーへ移る。**通信の種類を増やさずに済むよう、既存のメッセージを合図として使った**（プロトコル変更なし＝旧クライアントとの食い違いが起きない）

### 【Claude指摘・未確認】
実機2台での確認が必要。特に**ホストが「ルームに戻る」を押したときにゲストも一緒に戻るか**。

## クラウドを「草むら方式」にする（裁定59・ユーザー決定）2026-08-29
### 前提（調査結果）
**クラウドはこれまで視界を一切遮っていなかった。**半透明のグレーの円を1枚描いていただけで、その上にキャラも弾も普通に描かれていた。視界の仕組みはゲーム内のどこにも存在せず、botもクラウドを考慮していなかった。`SPEC.md` には「視界遮断のみ」とあるので、**仕様としては意図されていたが実装されないまま来た**もの。実質「見た目が濁るだけでゲージ30を払うスキル」で、ライトニング（スタン弾との合体技）の起点としてしか機能していなかった。

### ルール
- **味方と自分は常に見える。**クラウドは今までどおり透ける（ユーザー指定）
- **敵がクラウドの中にいると見えない**（本体も名前も描かない）
- **同じクラウドに自分も入っていれば見える。**踏み込んで暴くのが対処法
- **攻撃した直後（`revealMs` = 700ms）は見える。**撃ちながら完全に消えると、どこから撃たれたか分からず対処のしようがないため。`shoot` / `swing` / `hit` の3種で暴く
- **弾は隠さない。**弾は見えるので、そこから位置を推測するのが counterplay になる

### 【重要な限界】
**これはクライアント側の描画だけの処理。**中継サーバーは全員に同じ状態を配っているので、**クライアントを改造すれば透視できる。**身内で遊ぶ前提の割り切り。サーバー側で配る情報を絞る形にするなら、ランクモード（v2）と一緒に設計する必要がある。

### 【Claude指摘・要観察】
- クラウドの持続は4秒・半径180（裁定46）。**視界遮断が付いたので、同じ数値でも体感の強さは跳ね上がる。**強すぎたら持続を戻す
- botはクラウドを考慮しないので、**botは隠れた敵を撃ってくる。**訓練場やbot戦では不公平になる。気になるならbot側の索敵にも同じ判定を入れる必要がある

## ルーム設定のプリセットを3枠に（裁定60・ユーザー決定）2026-08-29
裁定57の単一保存を3枠に拡張。

- ロビー左側に **`枠1 / 枠2 / 枠3`** を縦に並べる（右のモード列と左右対称）
- **保存ボタンは作らない。**選んだ枠に対して、卓をいじるたび自動保存される
- **空の枠を選ぶと、今の卓のまま以後そこへ保存される**（＝別名保存として使える）。中身のある枠を選ぶとその構成に切り替わる
- 枠を切り替える前に**今の枠へ書き戻すので、押しても今の卓は失われない**
- ボタンには中身の要約（`枠1（bot3）` / `枠2（空）`）を出す
- **ホストのみ操作可。**bot構成とモードはホストの持ち物のため。ゲストには「ホストの設定が使われます」と出す
- 保存先は `dopagaki.room.v2`。**裁定57の `v1` が残っていれば1枠目として引き継ぐ**（前回の設定が消えないように）

## botの索敵にクラウドの可視判定を入れる（裁定61・ユーザー決定）2026-08-30
裁定59の「要観察」で挙げた **botが隠れた敵を撃ってくる** 問題への対応。

- 可視判定 `canSee(state, viewer, target)` を **`shared/src/sim/vision.ts` に移し、クライアント描画とbotの索敵が同じ関数を使う**（判定が2箇所に分かれてズレるのを防ぐ）
- そのために **「最後に攻撃した時刻」（`lastAttackAt`）をシミュレーション側で記録**する。これまでは描画側がイベントを見て自前で覚えていた。`shoot` / `swing` / `hit` の3種で更新し、判定は `state.t` 基準に統一
- botは**見える敵だけを狙う。**見える敵がいなくなったら撃たず、**最後に見た位置へ歩く**（＝踏み込んで暴く、というプレイヤーと同じ対処法）。24px以内に着いたら止まる
- ボスも同じ索敵を使う。ただし**扇状射撃はシミュレーション自動発動で「向いている方向」に撒くだけ**なので、隠れても弾は飛んでくる（狙いは付かない）

### 【Claude指摘・要観察】
- botがクラウドへ真っ直ぐ突っ込んでくる挙動になる。ルール上の正解だが「賢すぎる」と感じるなら、到達判定の距離や遅延を足す
- botが最後に見た位置に着いても敵が別のクラウドへ移っていれば棒立ちになる。一定時間で諦めて徘徊する処理は未実装

## ボス戦を段階制にする（裁定62・ユーザー決定）2026-08-30
引き継ぎメモ「ボス戦が間延びしないか」に対し、**回復ではなく段階制**を採用。

- HP比率が **66%** を割ると第2形態、**33%** を割ると第3形態（`BALANCE.boss.phases`）
- 移行の瞬間: **1秒無敵で仕切り直し＋CDを無視してノックバックの溜めを開始**（合図。溜めは見えるので逃げられる）。のけぞりも解除
- 形態ごとの強化: 扇状射撃 **5発/2.2秒 → 7発/1.8秒 → 9発/1.5秒**、ノックバックCD **7 → 6 → 5秒**。数値はすべて調整プレースホルダー
- 形態は上がるだけで戻らない（回復要素を後で足しても形態は維持）
- 画面: バナー「第N形態」、ボスHPバーに**閾値の目盛り**、名前の横に「第N形態」

### 【Claude指摘】
- **段階制は単調さの対策であって時短ではない。**むしろ後半が強くなるので試合は伸びる方向。長さ自体が問題なら `hpMultiplier` を下げる（実プレイ報告待ち）
- 移行時の無敵1秒の間に撃った弾は無駄になる。演出で分かるようにしてあるが、「当たらない」と感じたら無敵を短くする
- 第3形態の扇9発・44度は隙間が約5.5度。速い挑戦者なら抜けられる想定だが、**抜けられずに一方的に削られるなら `spreadRad` を広げる**

## タッチ射撃ボタンの拡大（裁定63・ユーザー要望）2026-08-30
射撃（main）の半径を **60 → 72**。右下隅からの位置を (-108,-135) → (-112,-140) に寄せて隣との間隔を保つ。

### 【Claude指摘・要実機確認】
サブ武器・スキル1との中心間距離は約130pxで、半径の合計（72+42 / 72+40）より余裕はある。ただし判定は半径+12pxまで拾うので、**「サブを押したつもりが射撃になる」が起きないか**見てほしい

## 弾幕モードを新設（裁定64・ユーザー決定）2026-08-30
東方風の弾幕避け。**ソロ専用・スピード限定・中央の動かない砲台を避けながら削る**（ユーザー決定: ソロ／撃破／剣とダッシュ両方可）。

### 構造
- モード `danmaku`。タイトルの「トレーニング」欄に「弾幕モード（スピード限定）」ボタン。訓練場と同じくリレー不要
- **訓練場ではない**（`practice=false`）: 残機3・制限時間180秒・決着あり。砲台を削り切れば勝ち、**時間切れは砲台の勝ち**
- 砲台はボス扱い（体1.6倍・HPは `danmaku.hpMultiplier`=6 で600。ボス戦の10とは別に持つ）。入力を持たず、シミュレーション側で自動射撃。**撃破回復はしない**（被弾で砲台が回復すると削りが進まないため）
- **弾に対する当たり判定を小さくする**（`hitRadius`=7px。東方式）。壁・場外・近接は通常の半径のまま。画面では白い点＋赤い輪で本体の位置を見せる
- **切り返し硬直（裁定36）をこのモードでは切る。**硬直は弾幕避けと真っ向から衝突するため
- 剣の弾消し（既存CD1.5秒）・ダッシュはそのまま使える

### 弾幕（形態制・裁定62を流用）
HP 66% / 33% で第2・第3形態。**移行時は無敵0.8秒＋砲台の弾を全消し**（スペルカード切替と同じ。仕切り直しの報酬）
| 形態 | リング（全周・撃つたび半歩ずらす） | 自機狙い | 渦（回転する腕） |
|---|---|---|---|
| 1 | 16発/1.6秒 | 3発・18度/1.1秒 | なし |
| 2 | 20発/1.3秒 | 3発/0.9秒 | 2本・10発/秒・80度/秒 |
| 3 | 24発/1.0秒 | 5発・30度/0.8秒 | 3本・12発/秒・110度/秒 |
数値はすべて調整プレースホルダー（`BALANCE.danmaku.phases`）。

### 【Claude指摘・要観察】
- **密度の手触りは未確認。**この環境でブラウザが起動できず（Chromiumの取得先がネットワーク許可外）、目視確認をしていない。**第1形態から順に「避けられる余地があるか」を実機で見てほしい**
- 近接で殴りに行くと自機狙いが至近距離で来る。剣の弾消しで凌げるかが「殴り」の成立条件。無理なら近接中は自機狙いを止める
- 剣が強すぎて「避ける」より「振る」が正解になるなら、弾消しCDを伸ばすかリングの弾を「消せない弾」にする
- 弾数が多いので低スペック端末で描画が重くなる可能性（処理落ち検知は既存）
- リザルトの「キャラ選択へ」は弾幕では「ホームに戻る」に差し替え。「続ける」は「もう一度」

## 弾幕モードの入口をタイトルで独立させる（裁定65・ユーザー要望）2026-08-30
裁定64で「トレーニング」欄に置いたボタンを、**フレンド対戦・トレーニングと並ぶ3枚目のパネル**に移した。

- タイトルは3列: 左「フレンド対戦」（x=215）／中央「弾幕モード」（x=640）／右「トレーニング」（x=1065）。パネル幅480→390
- 中央パネルに砲台とリング弾の簡易イラスト・説明3行・「挑戦する」ボタン。残機・制限時間は `BALANCE.danmaku` から表示（数値を変えても文言がずれない）
- トレーニング欄は裁定64前の配置に戻した

### 【Claude指摘・要実機確認】
- パネル幅が狭くなったので、ルームコード入力枠（幅320）とボタン（幅320）の左右余白が35pxずつしか無い。**窮屈に見えないか**
- 中央のイラストは描画だけで押せない。押せると誤解するなら枠を消す

## タイトルの並び替えと弾幕モードの難易度3段階（裁定66・ユーザー要望）2026-08-30
- タイトルの3列を **左「フレンド対戦」／中央「トレーニング」／右「弾幕モード」** に並び替え（裁定65の中央・右を入れ替え）
- 弾幕モードに難易度 **普通／難しい／地獄**。弾幕パネルのボタンで順ぐり切替、リザルトに難易度を表示
- 難易度は形態ごとの数値に**倍率**を掛ける方式（`BALANCE.danmaku.difficulties`）。形態×難易度で数値を9セット手書きすると調整が破綻するため
  - 難しい: 発数×1.25・回転×1.3・弾速×1.15
  - 地獄: 発数×1.5・回転×1.6・弾速×1.3
  - **残機・制限時間・砲台HPは変えない**（難しさは弾幕だけで作る）

### 【Claude指摘・要観察】
- **第3形態×地獄は掛け算で跳ね上がる**（リング36発/0.63秒・渦5本・弾速1.3倍）。ここが避けられない密度になっている可能性が最も高い。避けられないなら「地獄」の倍率を下げるか、第3形態だけ倍率を弱める
- 「難しい」と「地獄」の差が体感で分かるか。分からなければ「地獄」だけ弾速をさらに上げる
