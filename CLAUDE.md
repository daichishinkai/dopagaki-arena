# CLAUDE.md — ドパガキアリーナ（俯瞰2D・ホスト権威型）

## このプロジェクトについて
- ゲーム名「ドパガキアリーナ」（DOPAGAKI ARENA）。友達とルームコードで集まる俯瞰2D対戦アクション。ブラウザ配布、v1はPCのみ。
- **仕様の正典は `SPEC.md`（要件定義書v3.2）**。数値は `SPEC.md` 16章が最終値。迷ったら仕様を読む。仕様にないことは勝手に決めず、質問する。
- 開発者は**非エンジニア**。説明・報告は専門用語を避け、「何をしたか／どう確認するか」を日本語で簡潔に。

## 技術スタック（変更禁止）
- クライアント: Phaser 3 + TypeScript + Vite
- 中継サーバー: Node.js + `ws`（素のWebSocket）。Colyseus・WebRTCは使わない
- テスト: Vitest（純ロジックのみ）
- パッケージ管理: npm workspaces（`client` / `relay` / `shared`）

## リポジトリ構成
```
SPEC.md              仕様（正典）
docs/PLAN.md         現在のマイルストーンと残タスク
docs/DECISIONS.md    実装中に決めたことの記録（日付・理由）
shared/src/          ホストと非ホストで共通のシミュレーションコード
  balance.ts         全バランス数値。マジックナンバーはここ以外に書かない
  sim/               入力→状態更新の純関数。Phaserに依存しない
  net/               メッセージ型定義
client/src/          Phaser（描画・入力・ネット接続）
relay/src/           ステートレス中継のみ。ゲームルールを一切知らない
```

## よく使うコマンド
```
npm install                 依存インストール
npm run dev                 クライアント(5173)と中継(8080)を同時起動
npm run dev:client          クライアントのみ
npm run dev:relay           中継のみ
npm test                    Vitest（shared の純ロジック）
npm run typecheck           tsc --noEmit（全ワークスペース）
npm run build               本番ビルド
```

## アーキテクチャ原則（絶対）
- **ホスト権威**: 当たり判定・HP・ゲージ・bot はホスト機だけが計算する。非ホストは入力送信と補間のみ。
- **中継はステートレス**: ルーム表以外の状態を持たない。HPも弾も理解しない。DBなし。
- **シミュレーションは固定タイムステップ**（60Hz）で、描画（Phaser）と分離する。`shared/src/sim` は Phaser を import しない。
- ホストも非ホストも `shared` の同じコードを動かす。ホスト専用の分岐は `isHost` フラグで最小限に。
- 例外は仕様に書いてあるものだけ（例: セイバー弾消し判定は防御側クライアント）。

## 作業の進め方（必ず守る）
1. **3ファイル以上に触る変更はプランモードで計画を出し、承認を待つ**。1ファイルの小修正は直接やってよい。
2. **1回の作業＝1機能＝1コミット**。複数機能を混ぜない。
3. **完了報告には証拠を添える**: 実行したコマンドと出力、テスト結果、UIなら動作確認の手順。「動くはず」は禁止。
4. 計画から逸れる必要が出たら、**続行せず止まって理由を報告**する。
5. 数値を変えるときは `balance.ts` だけを変え、`SPEC.md` 16章の値と違う場合は `docs/DECISIONS.md` に理由を書く。
6. 仕様の曖昧さを見つけたら、推測で実装せず質問する（1回の質問は1件、選択肢つき）。
7. 作業の最後に `docs/PLAN.md` の残タスクを更新する。

## 検証の層
- 純ロジック（ダメージ計算・ゲージ収支・距離減衰・CC半減・残機）: **Vitestで必ずテスト**。仕様の数値例をそのままテストケースにする。
- 見た目・操作感: ブラウザで確認する手順を報告に書く（「npm run dev → 5173を開く → WASDで動く → 左クリックで弾が出る」）。
- ネット同期: ブラウザを2枚開き、片方でルーム作成・片方でコード参加し、両方で相手の動きが見えることを確認する手順を書く。

## コーディング規約
- TypeScript strict。`any` 禁止。
- 関数は小さく、`sim` 内は副作用なしの純関数。
- 命名は仕様の用語をそのまま使う（`escapeGauge`, `unifiedGauge`, `mark`, `overload`, `groundSlam`）。
- コメントは「なぜ」だけ書く。「何を」はコードで分かるようにする。
- 依存ライブラリの追加は事前に確認を取る。

## やらないこと
- v1範囲外の実装（層3合体技、スマホ対応、ランキング、ホスト移譲、ドット絵）。
- 認証・アカウント・DB。
- `SPEC.md` 17章の棄却済み案の再提案。
- 秘密情報（トークン等）のコミット。

## コンテキスト圧縮時に必ず残すこと
- 変更したファイル一覧、現在のマイルストーン、未解決の質問、テストコマンドとその最終結果。
